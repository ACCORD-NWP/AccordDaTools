# __init__.py

import logging
logging.basicConfig(level=logging.INFO)
log = logging.getLogger(__name__)

log.info('  TuneBR module : compute a posteriori diagnosed sigma for observation and background  ')
